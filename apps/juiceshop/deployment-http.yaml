---
# apps/juiceshop/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: juice-shop
  namespace: ${NS}
  labels:
    app: juice-shop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: juice-shop
  template:
    metadata:
      labels:
        app: juice-shop
    spec:
      containers:
        - name: juice-shop
          image: bkimminich/juice-shop:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: juice-shop
  namespace: ${NS}
  labels:
    app: juice-shop
spec:
  type: NodePort
  selector:
    app: juice-shop
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: ${HTTP_PORT}
